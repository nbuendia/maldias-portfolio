import { contactMeSlice } from "./ContactMe";

export { contactMeSlice };

export const {setShowContactSection, setShowContactAscii, setShowContactInfoSection, setShowContactInfo, setCurrentContactIndex} = contactMeSlice.actions;
